interface:
  display_name: "Main"
  short_description: "Repo-specific patterns and workflows for main"
  default_prompt: "Use the main repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true