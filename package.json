{
  "name": "main",
  "version": "1.0.0",
  "description": "A browser game built on a hand-rolled HTML5 Canvas isometric renderer. No libraries, no build step — open `index.html`.",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "http://localhost:26831/JavsonOf/main"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
